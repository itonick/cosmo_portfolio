# CLAUDE.md — cosmo_portfolio 運用ルール

## プロジェクト概要
スクロール駆動の 3D 個人ポートフォリオ「宇宙の神秘 3D ポートフォリオ（Cosmic Mystery 3D Portfolio）」。Three.js の連続した「宇宙の航行」を背景に、5 つの宇宙現象（六角形の嵐 / アインシュタイン・リング / スクエア・ネブラ / 黄金比の螺旋銀河 / 宇宙の大規模構造）へカメラが飛び込みながら、Hero/About/Skills/Projects/Contact の 5 セクションが流れる。トーンは「暗い・科学的・観測所/テレメトリ」。

### 設計リファレンス（`design/`）
- `design/README.md` — 自己完結のフルスペック（色・タイポ・余白・モーション timing・3D パラメータ・各現象の作り）。**一次情報。**
- `design/Cosmic_Portfolio.dc.html` — 挙動と厳密値を確認するためのプロトタイプ。`_buildStorm/_buildRing/_buildNebula/_buildSpiral/_buildWeb/_buildStars/_buildComets`・レンダーループ・カメラ・`_dispose` を含む。
- `design/support.js` — プロトタイプが依存する独自ランタイム。**参照専用。製品にバンドルしない（`.dc.html`/`support.js` は出荷しない）。**

実装はこの設計を、対象コードベースの流儀（React / Vue / Svelte / 素の TS + three 等）で再現する。シーンロジックはフレームワーク非依存でほぼ逐語移植でき、DOM/ラッパ（ref・ライフサイクル）だけ書き換える。

---

## モデル役割分担（このリポジトリの中心方針）

### 思想
- **Fable 起動前に机を整える**：収集・整形・チェックは全部下位モデルに済ませ、Fable には最終判断だけを渡す。過剰消費も過剰節約もしない。
- **Fable5** に任せるのは「**型がない × 失敗コストが大きい × 全体を見る必要がある**」判断だけ。
- **Opus4.8** … 日常の判断・レビュー・執筆。**メインセッションの既定モデル。**
- **Sonnet5** … 型が決まった量産・実行。
- **Haiku4.5** … 機械的な変換・検査。

### モデル ID 対応
| 呼称 | model エイリアス | 正式 ID |
|---|---|---|
| Fable5 | （メイン切替のみ） | `claude-fable-5` |
| Opus4.8 | `opus` | `claude-opus-4-8` |
| Sonnet5 | `sonnet` | `claude-sonnet-5` |
| Haiku4.5 | `haiku` | `claude-haiku-4-5-20251001` |

### 自動振り分け（サブエージェント＝①）
モデルは各エージェントの `model:` で固定。メインは Opus のまま、下記へ委譲すれば勝手に切り替わる。

| 委譲先 | モデル | 担当 | 呼び出しスキル（③） |
|---|---|---|---|
| `token-extractor` | Haiku4.5 | 設計値の 1:1 抽出・整形・色変換・ダミー置換 | `cosmo-tokens` |
| `checker` | Haiku4.5 | lint/型/build 実行・チェックリスト照合 | `cosmo-qa` |
| `builder` | Sonnet5 | 型に沿った実装量産（シーン移植・セクション・カード） | `cosmo-build` |
| `reviewer` | Opus4.8 | コードレビュー（判断・指摘） | `cosmo-review` |
| `writer` | Opus4.8 | コピー/ドキュメント/コミット・PR 文面 | （随時 / workflow 工程5） |

- **スキル（③）にはモデルを書かない。** スキルは上表のエージェントを呼ぶだけ。モデル指定は①に一元化する。
- **Workflow（②）** … 繰り返しの一連作業は `.claude/workflows/content-update.md` の工程表（工程ごとに model・effort 明記）に従う。

### effort の目安
Haiku（機械作業）＝ low、Sonnet（型量産）＝ medium、Opus レビュー＝ high・執筆＝ medium、Fable（メイン判断）＝ high〜max。

---

## メイン切替の提案ルール（重要）
メインセッションのモデルは仕様上 **Claude 自身では変更できない**。ユーザーが `/model` で切り替える。Claude は**提案までを担当**する。

### 「今 Fable に切り替えるべきです」を提案する条件
次の 3 つが揃った**単発の重い判断**に当たったとき：
1. **型がない**（なぞれる既存パターン/明示スペックがない）
2. **失敗コストが大きい**（広範囲に波及・巻き戻しが高くつく）
3. **全体を見る必要がある**（局所では決められない）

例：スタック選定・アプリ全体設計・シーン移植の全体方針、大規模リファクタ、技術方針の転換、reviewer が「全体設計の作り直しが要る」と申し送ったとき。

**提案の作法（机を整えてから渡す）：**
- 提案前に、収集・整形・チェックを下位モデルで済ませる：`token-extractor`（値・現状）・`checker`（lint/型/build・チェックリスト）で事実を揃え、`reviewer`（Opus）で論点を整理。
- 提案時は「何を Fable に判断してほしいか」「揃えた材料（要約・選択肢・トレードオフ）」「切替後に Fable がやること／やらなくてよいこと」を 1 メッセージで提示する。
- Fable のメインセッションでは**判断に集中**させ、周辺の収集・実装・検査を持ち込まない。

### 「Opus に戻していいです」を提案する条件
- Fable の一点判断が終わり、以降が日常判断・型量産・機械検査に落ちたとき。
- 戻したあとは Opus をメインに、量産は `builder`、検査は `checker`、レビューは `reviewer` へ委譲して進める。

---

## 単発・未確定は定義ファイル化しない
- **繰り返し発生すると分かった業務だけ**を①②③④に固定する。現状 固定済み：値抽出・型量産・レビュー・機械検査・コンテンツ更新パイプライン。
- 単発（初期のスタック選定・全体アーキ決定など）や、まだ型が見えていない業務は**定義ファイル化せず**、この思想ルールに従って都度判断する。**1 回やって、繰り返すと分かった時点で①〜③へ昇格**させる。
- Fable 相当の業務は原則メイン切替（単発）で扱い、サブエージェント化しない（全体文脈が要るため）。

---

## 開発・Git
- 作業ブランチ：`claude/cosmo-portfolio-model-roles-oap389`。変更はこのブランチで開発・コミット・プッシュする。
- コミット/プッシュはユーザー依頼時に行う。PR は明示依頼があるときだけ作成する。
- 実装時は既存コードのスタイル（命名・コメント密度・イディオム）に合わせ、Three.js のリソースは cleanup まで対で書く。
